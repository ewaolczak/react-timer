import Button from '../Button/Button';

const Timer = () => {
  return (
    <div>
      <Button>Start</Button>
      <Button>Stop</Button>
      <Button>Clear</Button>
    </div>
  );
};

export default Timer;
